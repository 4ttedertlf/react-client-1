import { Container} from "react-bootstrap";

const Blob = ({blob}) => {
  return (
    <Container className="gap-3">
          <div>Blob: {blob}
    </div>
    </Container>

  );
};

export default Blob;
